const disjuntores = [false, false, true, false, false, true, false, false];

for (let numDidJuntor = 0; numDidJuntor < disjuntores.length; numDidJuntor++) {
    if(disjuntores[numDidJuntor]){
        console.log(numDidJuntor);
    }
}